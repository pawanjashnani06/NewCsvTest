machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build --rm=false -t greenbankstub .
deployment:
  dev:
    branch: chore/circleci-build
    commands:
      - ./deploy.sh
